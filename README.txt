This software creates a .gif file from a complex-variable function.

Some parameters are taken to draw vertical and horizontal lines (initial Cartesian coordinates) and the image of these lines by the function given is calculated, ploted and returned as a .gif file.
